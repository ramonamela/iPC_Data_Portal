"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var runtype_1 = require("../runtype");
var show_1 = require("../show");
function Dictionary(value, key) {
    if (key === void 0) { key = 'string'; }
    return runtype_1.create(function (x) {
        if (x === null || x === undefined) {
            var a = runtype_1.create(x, { tag: 'dictionary', key: key, value: value });
            throw runtype_1.validationError("Expected " + show_1.default(a) + ", but was " + x);
        }
        if (typeof x !== 'object') {
            var a = runtype_1.create(x, { tag: 'dictionary', key: key, value: value });
            throw runtype_1.validationError("Expected " + show_1.default(a.reflect) + ", but was " + typeof x);
        }
        if (Object.getPrototypeOf(x) !== Object.prototype) {
            if (!Array.isArray(x)) {
                var a = runtype_1.create(x, { tag: 'dictionary', key: key, value: value });
                throw runtype_1.validationError("Expected " + show_1.default(a.reflect) + ", but was " + Object.getPrototypeOf(x));
            }
            else if (key === 'string')
                throw runtype_1.validationError("Expected dictionary, but was array");
        }
        for (var k in x) {
            // Object keys are always strings
            if (key === 'number') {
                if (isNaN(+k))
                    throw runtype_1.validationError("Expected dictionary key to be a number, but was string");
            }
            try {
                value.check(x[k]);
            }
            catch (_a) {
                var nestedKey = _a.key, message = _a.message;
                throw runtype_1.validationError(message, nestedKey ? k + "." + nestedKey : k);
            }
        }
        return x;
    }, { tag: 'dictionary', key: key, value: value });
}
exports.Dictionary = Dictionary;
